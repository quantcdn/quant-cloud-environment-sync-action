{
  "name": "quant-cloud-environment-sync-action",
  "version": "1.0.0",
  "description": "GitHub Action for syncing data between environments in Quant Cloud",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.ts",
    "test": "jest --config jest.config.unit.js",
    "test:unit": "jest --config jest.config.unit.js",
    "test:integration": "jest tests/index.test.ts",
    "test:mock": "./scripts/test-with-mock.sh",
    "prepare": "husky"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@quantcdn/quant-client": "^4.0.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "@vercel/ncc": "^0.38.3",
    "husky": "^9.1.7",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.0"
  }
}
